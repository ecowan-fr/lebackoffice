{% extends "_extends/base.html.twig" %}

{% block type_template %}
	{% if is_granted('IS_AUTHENTICATED_FULLY') %}
		<div class="ml-[102px]">
		{% endif %}
		{% block body '' %}
		{% if is_granted('IS_AUTHENTICATED_FULLY') %}
		</div>
	{% endif %}
{% endblock %}
