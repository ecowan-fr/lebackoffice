<img src="{{ asset(configs['structure.logo.url.light']) }}" alt="Logo lebackoffice">

<h1>Hi!</h1>

<p>To reset your password, please visit the following link</p>

<a href="{{ url('security.resetpassword.resetpassword', {token: resetToken.token}) }}">{{ url('security.resetpassword.resetpassword', {token: resetToken.token}) }}</a>

<p>This link will expire in
	{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
